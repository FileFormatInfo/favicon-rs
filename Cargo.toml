[package]
name = "favicon-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.5"
hyper = { version = "0.14", features = ["full"] }
tokio = { version = "1", features = ["full", "macros", "rt-multi-thread"] }
